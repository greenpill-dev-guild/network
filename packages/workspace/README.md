# @greenpill-network/workspace

Scaffold package for the future authenticated workspace at `app.greenpill.network`.

No workspace UI is implemented in this pass. This package exists to reserve the monorepo boundary so future member operations, auth/session surfaces, steward review tools, and collaborative workspace features do not get mixed into the root public Astro site.

