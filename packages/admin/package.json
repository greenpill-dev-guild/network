{
  "name": "@greenpill-network/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun ../../scripts/docker-compose.ts --env-file-if-exists ../../.env.local -f docker-compose.yml up admin-directus",
    "down": "bun ../../scripts/docker-compose.ts -f docker-compose.yml down",
    "compose:config": "bun ../../scripts/docker-compose.ts --env-file-if-exists ../../.env.local -f docker-compose.yml config"
  }
}
