# Reports

Store durable review outputs, matrices, or recommendation summaries here when they should live beside the hub.

## Current Reports

- `directus-fly-setup.md` records the Directus/Fly setup context for a separate private admin service.
- `directus-moderation-spike.md` evaluates Directus as a moderation layer for private map-node intake without adding a repo dependency.
